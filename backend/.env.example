GEMINI_API_KEY=
# Opsiyonel; varsayılan: gemini-2.5-flash (Vision destekler)
GEMINI_MODEL=gemini-2.5-flash

# Supabase (kombin geçmişi, profil, gardırop)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
# Opsiyonel: yalnızca legacy HS256 JWT secret kullanan eski projeler için
# Yeni Supabase (JWT Signing Keys / ES256) için gerekmez — JWKS ile doğrulanır
SUPABASE_JWT_SECRET=
